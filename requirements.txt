# Neural Video Compression & Streaming Engine - dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# The authoritative dependency list now lives in pyproject.toml, so the two
# cannot drift apart: this file installs the package itself (editable) plus the
# test and plotting extras. Installing the package is also what puts `nvc` on
# the import path, which the scripts and tests assume.
#
# IMPORTANT (torch / torchvision):
# pyproject pins versions that install a working CPU build on every platform. If
# you have an NVIDIA GPU and want CUDA acceleration matched to your driver
# version, follow the CUDA-specific install instructions in README.md BEFORE
# running this file, or re-run `pip install torch torchvision` with the index
# URL from https://pytorch.org/get-started/locally/ afterward to replace the CPU
# build.
#
# Runtime dependencies (from pyproject.toml): torch, torchvision, opencv-python,
# numpy, pytorch-msssim, tqdm.  dev extra: pytest.  research extra: matplotlib.

-e .[dev,research]
